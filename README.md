# Порівняльний аналіз алгоритмів сортування

## 📌 Опис завдання

Метою цього завдання було емпірично порівняти ефективність трьох алгоритмів сортування:

1. **Сортування вставками (Insertion Sort)** — простий алгоритм, ефективний для малих масивів.
2. **Сортування злиттям (Merge Sort)** — реалізація алгоритму "розділяй і володарюй".
3. **Timsort (Python `sorted`)** — гібридний алгоритм, вбудований у Python.

Тестування проводилося на наборах випадкових чисел різного розміру за допомогою модулів `timeit` та `random`.

## 📊 Результати вимірювань

Нижче наведено час виконання (в секундах) для кожного алгоритму на різних обсягах даних ($N$).

| Розмір масиву (N) | Insertion Sort (сек) | Merge Sort (сек) | Timsort (sorted) (сек) |
| :---------------: | :------------------: | :--------------: | :--------------------: |
|      **100**      |     _[0.000335]_     |   _[0.000196]_   |      _[0.000005]_      |
|      **500**      |     _[0.008668]_     |   _[0.001252]_   |      _[0.000035]_      |
|     **1000**      |     _[0.035671]_     |   _[0.003400]_   |      _[0.000085]_      |
|     **2000**      |     _[0.154743]_     |   _[0.006062]_   |      _[0.000198]_      |
|     **5000**      |     _[0.980003]_     |   _[0.012933]_   |      _[0.000586]_      |
|     **10000**     |     _[2.980950]_     |   _[0.027723]_   |      _[0.001690]_      |

## 🔎 Аналіз результатів

### 1. Insertion Sort (Сортування вставками)

- **Результат:** На масиві з 10,000 елементів час виконання склав майже **3 секунди** (2.98 с).
- **Висновки:** Алгоритм показує чітку квадратичну залежність $O(N^2)$. При збільшенні обсягу даних, час зростає вибухоподібно. Порівняно з Timsort, на 10,000 елементах Insertion Sort працює у **~1700 разів повільніше**. Це робить його непридатним для роботи з великими масивами.

### 2. Merge Sort (Сортування злиттям)

- **Результат:** На 10,000 елементах час склав всього **0.027 секунди**.
- **Висновки:** Алгоритм значно ефективніший за вставки. Він демонструє сублінійне зростання часу виконання $O(N \log N)$, що дозволяє йому ефективно обробляти великі масиви даних. Однак, він все ще повільніший за вбудований Timsort через накладні витрати інтерпретатора Python.

### 3. Timsort (Вбудований `sorted`)

- **Результат:** Абсолютний лідер з часом **0.00169 секунди** на 10,000 елементах.
- **Висновки:** Це **найшвидший** алгоритм. Він виявився швидшим за Merge Sort приблизно в **16 разів** і швидшим за Insertion Sort у **1763 рази** на максимальному наборі даних.

## 💡 Чому Timsort такий ефективний?

Попри те, що Merge Sort і Timsort мають однакову теоретичну складність $O(N \log N)$, вбудований алгоритм Python працює набагато швидше з двох причин:

1. **Гібридний підхід:** Timsort комбінує сортування вставками та сортування злиттям. Він розбиває масив на невеликі шматки ("runs") і сортує їх вставками (що дуже швидко для малих даних), а потім зливає ці шматки. Це дозволяє використовувати сильні сторони обох алгоритмів.
2. **Оптимізація на рівні мови:** Функція `sorted` реалізована на мові **C**, тоді як наш Merge Sort написаний на чистому Python. Компільований код C працює на порядки швидше за інтерпретований код Python.

## 🏆 Загальні висновки

Емпіричні дані підтвердили теоретичні оцінки складності алгоритмів.

- **Insertion Sort** ($O(N^2)$) є найповільнішим на великих даних.
- **Merge Sort** ($O(N \log N)$) є ефективним, але поступається вбудованим методам.
- **Timsort** є беззаперечним лідером.

Поєднання алгоритмічної досконалості (гібридний підхід) та низькорівневої оптимізації (реалізація на C) робить Timsort набагато ефективнішим. Саме тому програмісти використовують вбудовані функції сортування Python, а не пишуть власні реалізації з нуля: це економить час, ресурси процесора та гарантує надійність коду.
